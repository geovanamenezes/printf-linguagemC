
/*
 Use como exemplo o código abaixo 
 
	#include <stdio.h> 
 
	int main() {  
	 	int num1, num2, soma; 
		num1 = 10;   num2 = 5; 
 		soma = num1 + num2; 
 		printf(“soma = %d\n”, soma); 
 
  		return 0; 
		}
		
	Faça outros três códigos, um para:
	a) subtração; b) multiplicação; c) divisão.  
 
	Utilize variáveis float para o a letra c (divisão) 
	(falaremos mais sobre conversão de tipo de variáveis nas próximas aulas). 
*/
#include <stdio.h>
 int main(){
 	int num1, num2, subtracao;
 	num1 = 10;
 	num2 = 5;
 	subtracao = num1 - num2;
 	printf(" subtracao = %d ", subtracao);
 	
 	return 0;
 	
 }

